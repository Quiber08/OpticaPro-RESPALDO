<Page
    x:Class="OpticaPro.Views.OrdersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:OpticaPro.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <SolidColorBrush x:Key="StatusPendingBrush" Color="#D35400"/>
        <SolidColorBrush x:Key="StatusPendingBgBrush" Color="#20D35400"/>
        <SolidColorBrush x:Key="StatusDeliveredBrush" Color="#27AE60"/>
        <SolidColorBrush x:Key="StatusDeliveredBgBrush" Color="#2027AE60"/>
        <SolidColorBrush x:Key="StatusDebtBrush" Color="#C0392B"/>

        <Style x:Key="KpiCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
        </Style>

        <Style x:Key="FilterRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="RootBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlBackgroundBaseLowBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemAccentColor}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="White"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter x:Name="ContentPresenter" 
                                              Content="{TemplateBinding Content}" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              FontWeight="SemiBold"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GridActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Border Background="{ThemeResource SystemAccentColor}" Width="6" Height="32" CornerRadius="3"/>
                <TextBlock Text="Control de Pedidos" Style="{StaticResource TitleTextBlockStyle}" FontSize="28"/>
            </StackPanel>
            <TextBlock Text="Gestión integral de trabajos, laboratorio y entregas al paciente." 
                       Style="{StaticResource BodyTextBlockStyle}" 
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                       Margin="18,4,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource KpiCardStyle}" Width="240">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="En Taller / Pendientes" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" FontSize="12"/>
                    <TextBlock Grid.Row="1" x:Name="TxtPendingCount" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource StatusPendingBrush}"/>

                    <Ellipse Grid.Column="1" Grid.RowSpan="2" Width="48" Height="48" Fill="{StaticResource StatusPendingBgBrush}" VerticalAlignment="Top"/>
                    <FontIcon Grid.Column="1" Grid.RowSpan="2" Glyph="&#xE8F2;" FontSize="20" Foreground="{StaticResource StatusPendingBrush}" VerticalAlignment="Top" Margin="0,14,0,0"/>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource KpiCardStyle}" Width="240">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="Entregados (Total)" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" FontSize="12"/>
                    <TextBlock Grid.Row="1" x:Name="TxtDeliveredCount" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource StatusDeliveredBrush}"/>

                    <Ellipse Grid.Column="1" Grid.RowSpan="2" Width="48" Height="48" Fill="{StaticResource StatusDeliveredBgBrush}" VerticalAlignment="Top"/>
                    <FontIcon Grid.Column="1" Grid.RowSpan="2" Glyph="&#xE73E;" FontSize="20" Foreground="{StaticResource StatusDeliveredBrush}" VerticalAlignment="Top" Margin="0,14,0,0"/>
                </Grid>
            </Border>

            <StackPanel Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="8">
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,16" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="0">
                <RadioButton Content="Todos" GroupName="StatusFilter" IsChecked="True" Click="Filter_Click" Tag="All" Style="{StaticResource FilterRadioButtonStyle}"/>
                <RadioButton Content="En Taller" GroupName="StatusFilter" Click="Filter_Click" Tag="En Taller" Style="{StaticResource FilterRadioButtonStyle}"/>
                <RadioButton Content="Entregados" GroupName="StatusFilter" Click="Filter_Click" Tag="Entregado" Style="{StaticResource FilterRadioButtonStyle}"/>
            </StackPanel>

            <TextBox Grid.Column="2" x:Name="SearchBox" TextChanged="SearchBox_TextChanged" PlaceholderText="Buscar por nombre, CI o código..." CornerRadius="20">
                <TextBox.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Limpiar búsqueda" Click="ClearSearch_Click"/>
                    </MenuFlyout>
                </TextBox.ContextFlyout>
            </TextBox>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                    Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}" 
                    BorderThickness="1" 
                    BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
                    CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Padding="16,12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="180"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="FECHA" FontSize="11" FontWeight="Bold" Opacity="0.5"/>
                        <TextBlock Grid.Column="1" Text="CLIENTE / PACIENTE" FontSize="11" FontWeight="Bold" Opacity="0.5"/>
                        <TextBlock Grid.Column="2" Text="DETALLE PRODUCTO" FontSize="11" FontWeight="Bold" Opacity="0.5"/>
                        <TextBlock Grid.Column="3" Text="SALDO PEND." FontSize="11" FontWeight="Bold" Opacity="0.5" HorizontalAlignment="Right" Margin="0,0,16,0"/>
                        <TextBlock Grid.Column="4" Text="ESTADO" FontSize="11" FontWeight="Bold" Opacity="0.5" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="5" Text="ACCIONES RÁPIDAS" FontSize="11" FontWeight="Bold" Opacity="0.5" HorizontalAlignment="Center"/>
                    </Grid>

                    <ListView Grid.Row="1" x:Name="OrdersListView" SelectionMode="Single" Padding="0,0,0,24">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Margin" Value="4,2"/>
                                <Setter Property="Padding" Value="0"/>
                            </Style>
                        </ListView.ItemContainerStyle>

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Background="Transparent" Padding="12,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="180"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DeliveryDate}" FontWeight="SemiBold" FontSize="13"/>
                                        <TextBlock Text="Entrega" FontSize="10" Foreground="Gray"/>
                                    </StackPanel>

                                    <Grid Grid.Column="1" VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Width="36" Height="36" CornerRadius="18" Background="#200078D7" Margin="0,0,12,0">
                                            <FontIcon Glyph="&#xE77B;" Foreground="{ThemeResource SystemAccentColor}" FontSize="14"/>
                                        </Border>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding PatientName}" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis"/>
                                            <HyperlinkButton Content="Ver Historial" FontSize="10" Foreground="{ThemeResource SystemAccentColor}" Padding="0" Margin="0" BorderThickness="0"/>
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,16,0">
                                        <TextBlock Text="{Binding Frame}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="Armazón" FontSize="11" Foreground="Gray"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="3" Text="{Binding FormattedBalance}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,16,0"
                                               Foreground="{Binding BalanceColorBrush}" FontWeight="ExtraBold" FontSize="14"/>

                                    <Border Grid.Column="4" Background="{Binding StatusColorBrush}" CornerRadius="12" Padding="10,4" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.9">
                                        <TextBlock Text="{Binding Status}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                    </Border>

                                    <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Center">

                                        <Button Style="{StaticResource GridActionButtonStyle}" Click="BtnWhatsApp_Click" Tag="{Binding}" ToolTipService.ToolTip="Enviar WhatsApp">
                                            <Button.Content>
                                                <FontIcon Glyph="&#xE8BD;" FontSize="16" Foreground="#25D366"/>
                                            </Button.Content>
                                        </Button>

                                        <Button Style="{StaticResource GridActionButtonStyle}" Click="BtnEdit_Click" Tag="{Binding}" ToolTipService.ToolTip="Editar Pedido">
                                            <Button.Content>
                                                <FontIcon Glyph="&#xE70F;" FontSize="16" Foreground="Gray"/>
                                            </Button.Content>
                                        </Button>

                                        <Button Style="{StaticResource GridActionButtonStyle}" Click="BtnAbonar_Click" Tag="{Binding}" ToolTipService.ToolTip="Registrar Pago">
                                            <Button.Content>
                                                <FontIcon Glyph="&#xE8C7;" FontSize="16" Foreground="{ThemeResource SystemAccentColor}"/>
                                            </Button.Content>
                                        </Button>

                                        <Button Style="{StaticResource GridActionButtonStyle}" Click="BtnEntregar_Click" Tag="{Binding}" ToolTipService.ToolTip="Marcar Entregado">
                                            <Button.Content>
                                                <FontIcon Glyph="&#xE74C;" FontSize="16" Foreground="#0078D7"/>
                                            </Button.Content>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <StackPanel x:Name="EmptyStatePanel" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                        <FontIcon Glyph="&#xE721;" FontSize="64" Foreground="#E0E0E0"/>
                        <TextBlock Text="No se encontraron pedidos" FontSize="18" FontWeight="SemiBold" Foreground="Gray" HorizontalAlignment="Center" Margin="0,16,0,0"/>
                        <TextBlock Text="Intenta ajustar los filtros o la búsqueda." FontSize="14" Foreground="Gray" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</Page>