<Page
    x:Class="OpticaPro.Views.PrescriptionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:OpticaPro.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer>
            <StackPanel Padding="24" Spacing="24">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="4">
                        <TextBlock Text="Generar Informes y Recetas" Style="{StaticResource TitleTextBlockStyle}"/>
                        <TextBlock Text="Seleccione un paciente para ver su historial completo o imprimir."
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom">
                        <AutoSuggestBox PlaceholderText="Buscar por nombre o DNI..." 
                                        QueryIcon="Find" 
                                        Width="300" 
                                        TextChanged="SearchBox_TextChanged"/>
                    </StackPanel>
                </Grid>

                <StackPanel x:Name="EmptyState" Spacing="12" Visibility="Collapsed">
                    <Border CornerRadius="8" Padding="40" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                        <StackPanel HorizontalAlignment="Center" Spacing="16">
                            <Ellipse Width="60" Height="60" Fill="{ThemeResource SystemControlBackgroundBaseLowBrush}"/>
                            <FontIcon Glyph="&#xE721;" FontSize="24" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,-42,0,0"/>
                            <TextBlock Text="No se encontraron pacientes." FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <ListView x:Name="PrescriptionsList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Patient">
                            <Border Margin="0,0,0,12" CornerRadius="8" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Ellipse Width="48" Height="48" Fill="{ThemeResource SystemAccentColorLight2}"/>
                                    <FontIcon Glyph="&#xE77B;" FontSize="20" Foreground="{ThemeResource SystemAccentColorDark1}"/>

                                    <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind FullName}" FontWeight="SemiBold" FontSize="16"/>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xE82D;" FontSize="12" Foreground="Gray"/>
                                            <TextBlock Text="{x:Bind Dni}" FontSize="12" Foreground="Gray"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,24,0" Orientation="Horizontal" Spacing="16">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE717;" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            <TextBlock Text="{x:Bind Phone}" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="8">
                                        <Button Click="OpenReport_Click" Tag="{x:Bind}" ToolTipService.ToolTip="Generar Informe / Receta" Style="{StaticResource AccentButtonStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <FontIcon Glyph="&#xE749;" FontSize="16"/>
                                                <TextBlock Text="Crear Informe"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>